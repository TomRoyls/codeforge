import { describe, expect, test, vi } from 'vitest'
import { noUnnecessaryNumberIsFiniteSpreadRule } from '../../../../src/rules/patterns/no-unnecessary-number-is-finite-spread.js'
import type { RuleContext } from '../../../../src/plugins/types.js'

interface ReportDescriptor { message: string; loc?: { end: { column: number; line: number }; start: { column: number; line: number } }; node?: unknown }
function makeLoc(sl: number, sc: number, el: number, ec: number) { return { start: { line: sl, column: sc }, end: { line: el, column: ec } } }
function createMockContext(): { context: RuleContext; reports: ReportDescriptor[] } {
  const reports: ReportDescriptor[] = []
  const context: RuleContext = { report: (d: ReportDescriptor) => { reports.push({ message: d.message, loc: d.loc, node: d.node }) }), getFilePath: () => '/src/file.ts', getAST: () => null, getSource: () => '[]', getTokens: () => [], getComments: () => [], config: { options: [{}] }, logger: { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() }, workspaceRoot: '/src' } as unknown as RuleContext
  return { context, reports }
}
function makeNumberIsFiniteCall(args: unknown[] = [], sl = 1, sc = 0, el = 1, ec = 20): unknown {
  return { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: args, loc: makeLoc(sl, sc, el, ec) }
}
function makeSpreadArg(argument: unknown): unknown { return { type: 'SpreadElement', argument } }

describe('no-unnecessary-number-is-finite-spread rule', () => {
  describe('meta', () => {
    test('should have correct type', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.type).toBe('suggestion') })
    test('should have severity warn', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.severity).toBe('warn') })
    test('should have category patterns', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.docs.category).toBe('patterns') })
    test('should not be recommended', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.docs.recommended).toBe(false) })
    test('should have empty schema', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.schema).toEqual([]) })
    test('should have docs url', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.docs.url).toBeDefined() })
    test('should have description', () => { expect(noUnnecessaryNumberIsFiniteSpreadRule.meta.docs.description).toBeDefined() })
    test('should have string description', () => { expect(typeof noUnnecessaryNumberIsFiniteSpreadRule.meta.docs.description).toBe('string') })
  })

  describe('edge cases', () => {
    test('should not report on empty arguments', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([])); expect(reports).toHaveLength(0) })
    test('should not report on two regular arguments', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([{ type: 'Literal', value: 1 }, { type: 'Literal', value: 2 }])); expect(reports).toHaveLength(0) })
  })

  describe('should not report with wrong object name', () => {
    test('foo.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'foo' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('bar.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'bar' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('baz.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'baz' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('qux.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'qux' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('obj.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'obj' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('arr.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'arr' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('fn.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'fn' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('cb.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'cb' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('x.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'x' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('y.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'y' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('z.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'z' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('a.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'a' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('b.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'b' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('c.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'c' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('d.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'd' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('e.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'e' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('f.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'f' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('g.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'g' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('h.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'h' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('i.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'i' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('j.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'j' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('k.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'k' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('l.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'l' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('m.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'm' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('n.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'n' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('o.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'o' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('p.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'p' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('q.isFinite(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'q' }, property: { type: 'Identifier', name: 'isFinite' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
  })

  describe('should not report with wrong property name', () => {
    test('Number.foo(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'foo' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.bar(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'bar' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.baz(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'baz' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.qux(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'qux' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.toString(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'toString' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.valueOf(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'valueOf' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.hasOwnProperty(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'hasOwnProperty' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.constructor(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'constructor' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.prototype(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'prototype' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.__proto__(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: '__proto__' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.apply(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'apply' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.bind(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'bind' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.call(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'call' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.length(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'length' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.name(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'name' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.args(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'args' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.callee(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'callee' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.caller(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'caller' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.arguments(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'arguments' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.pop(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'pop' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.push(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'push' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.shift(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'shift' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.unshift(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'unshift' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.slice(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'slice' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.splice(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'splice' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.concat(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'concat' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.join(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'join' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.indexOf(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'indexOf' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.lastIndexOf(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'lastIndexOf' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.forEach(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'forEach' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.map(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'map' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.filter(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'filter' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.reduce(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'reduce' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.reduceRight(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'reduceRight' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.some(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'some' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.every(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'every' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.find(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'find' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.findIndex(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'findIndex' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.includes(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'includes' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
    test('Number.sort(...items) should not report', () => { const { context, reports } = createMockContext(); const node = { type: 'CallExpression', callee: { type: 'MemberExpression', object: { type: 'Identifier', name: 'Number' }, property: { type: 'Identifier', name: 'sort' }, computed: false }, arguments: [makeSpreadArg({ type: 'Identifier', name: 'items' })], loc: makeLoc(1, 0, 1, 20) }; noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(node); expect(reports).toHaveLength(0) })
  })

  describe('should report Number.isFinite(...items) with single spread', () => {
    test('should report case 1', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 1, 0, 1, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 2', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'arr' })], 2, 0, 2, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 3', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'args' })], 3, 0, 3, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 4', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'list' })], 4, 0, 4, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 5', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'data' })], 5, 0, 5, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 6', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'values' })], 6, 0, 6, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 7', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'nums' })], 7, 0, 7, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 8', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'rest' })], 8, 0, 8, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 9', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'options' })], 9, 0, 9, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 10', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'params' })], 10, 0, 10, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 11', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'collection' })], 11, 0, 11, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 12', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'elements' })], 12, 0, 12, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 13', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'entries' })], 13, 0, 13, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 14', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'objs' })], 14, 0, 14, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 15', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'source' })], 15, 0, 15, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 16', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'input' })], 16, 0, 16, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 17', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'payload' })], 17, 0, 17, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 18', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'buffer' })], 18, 0, 18, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 19', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'chunk' })], 19, 0, 19, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 20', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'segment' })], 20, 0, 20, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 21', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'portion' })], 21, 0, 21, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 22', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'range' })], 22, 0, 22, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 23', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'tuple' })], 23, 0, 23, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 24', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'seq' })], 24, 0, 24, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 25', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'iter' })], 25, 0, 25, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 26', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'result' })], 26, 0, 26, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 27', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'output' })], 27, 0, 27, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 28', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'response' })], 28, 0, 28, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 29', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'records' })], 29, 0, 29, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 30', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'rows' })], 30, 0, 30, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 31', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'cols' })], 31, 0, 31, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 32', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'cells' })], 32, 0, 32, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 33', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'fields' })], 33, 0, 33, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 34', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'props' })], 34, 0, 34, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 35', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'attrs' })], 35, 0, 35, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 36', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'keys' })], 36, 0, 36, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 37', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'vals' })], 37, 0, 37, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 38', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'pairs' })], 38, 0, 38, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 39', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'nodes' })], 39, 0, 39, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
    test('should report case 40', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items2' })], 40, 0, 40, 25)); expect(reports).toHaveLength(1); expect(reports[0].message).toBeDefined() })
  })

  describe('location and structure', () => {
    test('location line 2', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 2, 5, 2, 30)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(2); expect(reports[0].loc?.start.column).toBe(5); expect(reports[0].loc?.end.line).toBe(2); expect(reports[0].loc?.end.column).toBe(30) })
    test('location line 3', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 3, 10, 3, 35)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(3); expect(reports[0].loc?.start.column).toBe(10); expect(reports[0].loc?.end.line).toBe(3); expect(reports[0].loc?.end.column).toBe(35) })
    test('location line 5', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 5, 0, 5, 20)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(5); expect(reports[0].loc?.start.column).toBe(0); expect(reports[0].loc?.end.line).toBe(5); expect(reports[0].loc?.end.column).toBe(20) })
    test('location line 10', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 10, 8, 10, 28)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(10); expect(reports[0].loc?.start.column).toBe(8); expect(reports[0].loc?.end.line).toBe(10); expect(reports[0].loc?.end.column).toBe(28) })
    test('location line 15', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 15, 3, 15, 23)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(15); expect(reports[0].loc?.start.column).toBe(3); expect(reports[0].loc?.end.line).toBe(15); expect(reports[0].loc?.end.column).toBe(23) })
    test('location line 20', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 20, 0, 20, 15)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(20); expect(reports[0].loc?.start.column).toBe(0); expect(reports[0].loc?.end.line).toBe(20); expect(reports[0].loc?.end.column).toBe(15) })
    test('location line 25', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 25, 12, 25, 37)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(25); expect(reports[0].loc?.start.column).toBe(12); expect(reports[0].loc?.end.line).toBe(25); expect(reports[0].loc?.end.column).toBe(37) })
    test('location line 30', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 30, 1, 30, 21)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(30); expect(reports[0].loc?.start.column).toBe(1); expect(reports[0].loc?.end.line).toBe(30); expect(reports[0].loc?.end.column).toBe(21) })
    test('location line 40', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 40, 5, 40, 25)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(40); expect(reports[0].loc?.start.column).toBe(5); expect(reports[0].loc?.end.line).toBe(40); expect(reports[0].loc?.end.column).toBe(25) })
    test('location line 50', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 50, 0, 50, 30)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(50); expect(reports[0].loc?.start.column).toBe(0); expect(reports[0].loc?.end.line).toBe(50); expect(reports[0].loc?.end.column).toBe(30) })
    test('location line 60', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 60, 7, 60, 27)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(60); expect(reports[0].loc?.start.column).toBe(7); expect(reports[0].loc?.end.line).toBe(60); expect(reports[0].loc?.end.column).toBe(27) })
    test('location line 70', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 70, 2, 70, 22)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(70); expect(reports[0].loc?.start.column).toBe(2); expect(reports[0].loc?.end.line).toBe(70); expect(reports[0].loc?.end.column).toBe(22) })
    test('location line 80', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 80, 0, 80, 20)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(80); expect(reports[0].loc?.start.column).toBe(0); expect(reports[0].loc?.end.line).toBe(80); expect(reports[0].loc?.end.column).toBe(20) })
    test('location line 90', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 90, 15, 90, 40)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(90); expect(reports[0].loc?.start.column).toBe(15); expect(reports[0].loc?.end.line).toBe(90); expect(reports[0].loc?.end.column).toBe(40) })
    test('location line 100', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 100, 0, 100, 25)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(100); expect(reports[0].loc?.start.column).toBe(0); expect(reports[0].loc?.end.line).toBe(100); expect(reports[0].loc?.end.column).toBe(25) })
    test('location line 150', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 150, 3, 150, 23)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(150); expect(reports[0].loc?.start.column).toBe(3); expect(reports[0].loc?.end.line).toBe(150); expect(reports[0].loc?.end.column).toBe(23) })
    test('location line 200', () => { const { context, reports } = createMockContext(); noUnnecessaryNumberIsFiniteSpreadRule.create(context).CallExpression!(makeNumberIsFiniteCall([makeSpreadArg({ type: 'Identifier', name: 'items' })], 200, 8, 200, 33)); expect(reports).toHaveLength(1); expect(reports[0].loc?.start.line).toBe(200); expect(reports[0].loc?.start.column).toBe(8); expect(reports[0].loc?.end.line).toBe(200); expect(reports[0].loc?.end.column).toBe(33) })
  })
})